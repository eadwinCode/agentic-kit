{
  "name": "agentic-kit",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "scripts": {
    "typecheck": "bun run --cwd packages/agentic-kit typecheck && bun run --cwd packages/use-agentkit typecheck",
    "build": "bun run --cwd packages/agentic-kit build && bun run --cwd packages/use-agentkit build",
    "test": "bun run --cwd packages/agentic-kit test && bun run --cwd packages/use-agentkit test",
    "docs:dev": "vitepress dev docs",
    "docs:build": "vitepress build docs",
    "docs:preview": "vitepress preview docs"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitepress": "^1.6.4"
  }
}
