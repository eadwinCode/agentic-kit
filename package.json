{
  "name": "agentic-kit",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*",
    "examples/*",
    "examples/*/web"
  ],
  "scripts": {
    "typecheck": "bun run --cwd packages/agentenkit typecheck && bun run --cwd packages/use-agentenkit typecheck",
    "build": "bun run --cwd packages/agentenkit build && bun run --cwd packages/use-agentenkit build",
    "test": "bun run --cwd packages/agentenkit test && bun run --cwd packages/use-agentenkit test",
    "docs:dev": "vitepress dev docs",
    "docs:build": "vitepress build docs",
    "docs:preview": "vitepress preview docs"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitepress": "^1.6.4"
  }
}
