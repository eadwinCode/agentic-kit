{
  "name": "agentic-kit",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "scripts": {
    "typecheck": "bun run --cwd packages/agentrun typecheck && bun run --cwd packages/use-agentrun typecheck",
    "build": "bun run --cwd packages/agentrun build && bun run --cwd packages/use-agentrun build",
    "test": "bun run --cwd packages/agentrun test && bun run --cwd packages/use-agentrun test",
    "docs:dev": "vitepress dev docs",
    "docs:build": "vitepress build docs",
    "docs:preview": "vitepress preview docs"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitepress": "^1.6.4"
  }
}
